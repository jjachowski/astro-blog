---
type Props = {
    platform: string;
    username: string;
};
const { platform, username } = Astro.props;
---

<a
    class="dark:text-pink-500 text-pink-600 underline hover:dark:text-indigo-500 hover:text-indigo-600"
    href={`https://www.${platform}.com/${username}`}>{platform}</a
>
